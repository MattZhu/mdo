package com.mdo.domainobject;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;
import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.mdo.domainobject.SymptomApplication;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table SYMPTOM_APPLICATION.
*/
public class SymptomApplicationDao extends AbstractDao<SymptomApplication, Long> {

    public static final String TABLENAME = "SYMPTOM_APPLICATION";

    /**
     * Properties of entity SymptomApplication.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property SymId = new Property(1, Integer.class, "SymId", false, "SYM_ID");
        public final static Property SymbolCode = new Property(2, String.class, "SymbolCode", false, "SYMBOL_CODE");
        public final static Property Description = new Property(3, String.class, "Description", false, "DESCRIPTION");
        public final static Property ApplicationType = new Property(4, Integer.class, "ApplicationType", false, "APPLICATION_TYPE");
        public final static Property AdditionalSymbols = new Property(5, String.class, "AdditionalSymbols", false, "ADDITIONAL_SYMBOLS");
    };


    public SymptomApplicationDao(DaoConfig config) {
        super(config);
    }
    
    public SymptomApplicationDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'SYMPTOM_APPLICATION' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'SYM_ID' INTEGER," + // 1: SymId
                "'SYMBOL_CODE' TEXT," + // 2: SymbolCode
                "'DESCRIPTION' TEXT," + // 3: Description
                "'APPLICATION_TYPE' INTEGER," + // 4: ApplicationType
                "'ADDITIONAL_SYMBOLS' TEXT);"); // 5: AdditionalSymbols
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'SYMPTOM_APPLICATION'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, SymptomApplication entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer SymId = entity.getSymId();
        if (SymId != null) {
            stmt.bindLong(2, SymId);
        }
 
        String SymbolCode = entity.getSymbolCode();
        if (SymbolCode != null) {
            stmt.bindString(3, SymbolCode);
        }
 
        String Description = entity.getDescription();
        if (Description != null) {
            stmt.bindString(4, Description);
        }
 
        Integer ApplicationType = entity.getApplicationType();
        if (ApplicationType != null) {
            stmt.bindLong(5, ApplicationType);
        }
 
        String AdditionalSymbols = entity.getAdditionalSymbols();
        if (AdditionalSymbols != null) {
            stmt.bindString(6, AdditionalSymbols);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public SymptomApplication readEntity(Cursor cursor, int offset) {
        SymptomApplication entity = new SymptomApplication( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // SymId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // SymbolCode
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // Description
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // ApplicationType
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5) // AdditionalSymbols
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, SymptomApplication entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setSymId(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setSymbolCode(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setDescription(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setApplicationType(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setAdditionalSymbols(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(SymptomApplication entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(SymptomApplication entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
